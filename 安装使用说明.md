# Kickstarter 追踪器 —— 安装与使用说明

> 这份文档是给**收到安装包的人**看的，不需要任何技术背景。

---

## 一、安装

1. 双击 `Kickstarter追踪器-安装包-1.0.0.exe`
2. 如果 Windows 弹出蓝色的「**已保护你的电脑**」提示：
   - 点左边的「**更多信息**」
   - 再点「**仍要运行**」
   - 这是因为安装包没有购买数字签名证书，不是病毒。
3. 选择安装位置（默认即可），点「安装」
4. 安装完成后，**桌面**和**开始菜单**都会出现「Kickstarter Tracker」图标

安装过程不需要管理员权限，也不会修改系统设置。

---

## 二、第一次打开

双击桌面图标即可。首次启动会花 **30～60 秒** 做一次网络校验（这是 Kickstarter 的
网站防护机制要求的），期间界面可能显示「正在抓取…」，属正常现象，耐心等一下。

之后启动会快很多。

---

## 三、主要功能

### 1. 添加众筹项目

把 Kickstarter 的项目网址粘贴到输入框，点「添加」。

支持的项目网址形如：

```
https://www.kickstarter.com/projects/创建者名/项目名
```

⚠️ 必须**具体项目页**的网址。首页、分类页、搜索页都不行。

最多可以同时追踪 **3 个**项目。

### 2. 浮窗

浮窗是一个小窗口，专门用来随时瞟一眼筹款金额，有三种模式：

| 模式 | 效果 |
|---|---|
| **隐藏** | 不显示浮窗 |
| **始终置顶** | 浮窗压在其它窗口上面，看视频、写文档时也能看到 |
| **仅桌面显示** | 浮窗只在桌面可见，切到别的程序就自动让开，不挡视线 |

在**主界面顶部**或**右键系统托盘图标**都能切换。

浮窗可以直接拖动到屏幕任意位置，位置会自动记住。

#### 浮窗外观：iOS 风格的毛玻璃

浮窗是**半透明的毛玻璃**，观感参考 iOS 的材质：背后的桌面和窗口会被真实地
模糊掉，并且颜色会被**压低饱和度**，变成一层雾面、哑光的色块 ——
能看出背后「有东西」，但认不出具体形状。这正是 iOS 毛玻璃的质感。

这个效果由两层叠加而成，**恒定生效，不需要任何设置**：

| 层 | 由谁提供 | 作用 |
|---|---|---|
| **真实模糊** | 系统原生亚克力背板 | 让背后的界面**真的被模糊**，实时透出来 |
| **iOS 质感** | 界面样式层 | 降饱和（不是提饱和）、轻微提亮、清晰的边缘内描边 |

> 模糊程度由系统控制，跟在不在「开发者模式」下运行无关。

#### 唯一的外观调节：浮窗底色浓度

浮窗外观**只有一个可调项** —— 主界面顶部的 **「浮窗底色」滑块**，
可在 **0% ～ 85%** 之间无级调节。这个数字代表**底色遮罩的浓度**：

- **0%** —— 底色完全消失，背板的模糊最明显（背后最朦胧）。壁纸是深色时最好看。
- **45%** —— 默认值。底色适中，背后模糊被遮住较多，糊感较弱、背后更清晰；文字衬底也充足。
- **85%** —— 上限。再高就把系统模糊好的背景盖死了，「毛玻璃」就不成立。

拖动时立即生效，不用重启。滑块步进为 5%。
**数值越大，背后越清晰（模糊被遮住越多）；数值越小，背后越朦胧（模糊透出越多）。**
数字看不清时往上拖，想要更透时往下拖。
**无论拖到哪个值，背后的模糊和 iOS 那层降饱和质感都一直在。**

> ⚠️ 如果拖到某个值后觉得「只看到颜色变深变浅、完全没有玻璃感」，
> 说明系统背板没生效（旧版有这个问题，已在当前版本修复）。
> 参见下方「毛玻璃没生效时怎么排查」。

> **之前版本里的「毛玻璃 / 半透明」档位已经删除** —— 玻璃恒定生效，
> 不再需要你选。如果你的界面里还有这两个按钮，说明用的是旧版。
>
> 浮窗也**没有「不透明」这个选项** —— 它存在的意义就是随时瞟一眼金额，
> 做成实色会挡住底下的内容。

> 原生毛玻璃需要 **Windows 11 22H2 及以上**。如果你的系统较老，
> 它会**自动**降级（加大样式层的模糊半径与降饱和来补偿），
> 外观依然流畅好看，只是背景不会被真实模糊。这是自动的，无需设置。

#### 毛玻璃没生效时怎么排查

如果浮窗看起来像一块**实色板**（拖动底色滑块只改变颜色深浅，
完全看不到背后的画面），按顺序检查：

1. **系统版本** —— 按 `Win + R` 输入 `winver`，看内部版本号是否 ≥ 22621
   （即 Windows 11 22H2 及以上）。低于这个版本只能走降级路径。
2. **查看日志** —— 在程序目录下的 `start.log` 里搜索 `[glass]`，
   正常应该看到这样的行：
   ```
   [glass] 浮窗毛玻璃：opacity=45% ok=true method=system-backdrop
           backdropValue=3 verified=true intensified=false exStyle=0x80108
   ```
   关键是 **`backdropValue=3`** 和 **`verified=true`**：
   - `backdropValue=3` 表示系统背板材质 = Acrylic（可用）
   - `backdropValue=0` 表示背板**没有真正生效**（旧版本就有这个问题）
   - `exStyle` 里应含 `80000` 位（即 `WS_EX_LAYERED`），这是背板生效的前提
3. **把上面的日志发给我** —— 只要这两行就能定位到具体环节。

### 3. 主界面也带背景毛玻璃

主界面（控制面板）同样做了两件事：

- 底色是 **85% 不透明的 `#191919`**（外加一层 iOS 风格的轻微提亮）
- 对**界面背后的其他应用**有实时 blur 模糊效果，并把颜色压低饱和度

也就是说，把主界面拖到浏览器或其他软件上面时，能透过它看到背后被模糊、
被去饱和的画面，和浮窗是同一套机制。

顶部工具栏、底部状态栏、以及**每一张项目卡片**都**是半透明的**，
不会把玻璃挡成色带 —— **整窗都能透出背后的模糊画面**，
没有哪一块是死实的。

### 4. 界面没有小图标

界面上的所有装饰性小图标都已去掉，按钮**一律是纯文字**，
看起来更干净、也更符合扁平化风格。

### 5. 金额单位

**统一以美元（US$）显示。**

Kickstarter 会根据你所在的地区自动切换显示货币（比如在国内可能显示成新加坡币），
所以本应用显示的金额可能和你在网页上看到的数字**不一样** —— 这是正常的，
本应用固定读取官方的美元换算数据，不受地区影响。

### 6. 增长折线图

应用会**每 2 小时**自动记录一次金额，并在详情里画出折线图，可以看到：

- 24 小时内 / 7 天 / 30 天 / 全部时间的走势
- 每个区间的**增长金额**和**增长百分比**

每个项目的数据最长**保留 50 天**（约 600 条记录）。超过 50 天的旧记录会被
自动清理，让数据文件不会无限膨胀。50 天的曲线足够看清一个众筹项目的完整节奏 ——
从冷启动、爆发期到收尾。

刚添加的项目只有起始点，运行几小时后曲线才会逐渐展开。

**把鼠标移到折线图上的数据点**，指针旁边会弹出一个小卡片，显示这个数据点的
**记录时间、众筹金额、支持者人数**。同时这个点会放大并加一圈白色描边，
往上还会画一条虚线，方便看清指的是哪一个点。鼠标移开就自动收起。

### 7. 跳转到网页

在项目卡片上**点一下**，就会用默认浏览器打开该项目在 Kickstarter 的页面。

### 8. 界面外观

界面是**iOS 风格深色毛玻璃**，底色 `#191919`（带 85% 不透明度和实时背景毛玻璃），
主色是亮黄绿 `#ddf813`。

顶部没有传统的 **文件 / 编辑 / 视图 / 窗口 / 帮助** 菜单栏，所有操作都在界面上直接点。

---

## 四、常见问题

**Q：双击没反应 / 一闪就没了？**
A：先确认是不是被杀毒软件拦截了。如果反复出现，把日志文件发给开发者，
里面记录了启动的每一步。日志位置（安装版）：

```
%APPDATA%\kickstarter-tracker\start.log
```

在文件资源管理器地址栏粘贴上面这行回车即可打开该文件夹
（`%APPDATA%` 会自动展开成你的用户目录）。日志开头会打印它自己的完整路径。

**Q：界面一直显示「正在抓取…」不结束？**
A：多半是网络问题。确认能正常访问 kickstarter.com，然后等 1～2 分钟。
期间不用点来点去。

**Q：添加项目时提示「无法获取项目数据」？**
A：检查三点：
1. 网址是不是**具体项目页**（而不是首页/分类页）
2. 网络是否正常
3. 该项目是否已被删除或设为私密

**Q：浮窗不见了？**
A：可能切到了「隐藏」模式。右键系统托盘的图标，选「浮窗：始终置顶」。

**Q：毛玻璃效果没出来，浮窗是实色的？**
A：检查两点：
1. **浮窗不透明度滑块是不是拖得太靠右**（85% 以上会把模糊盖死）
2. 系统是不是 **Windows 11 22H2（build 22621）及以上**
   —— 低于这个版本的系统没有原生背板 API，会**自动**降级
   （样式层的模糊与降饱和会加大补偿，但背后的画面不会被真实模糊）

按 `Win + R` 输入 `winver` 可以看系统版本。若是 Win10，属正常情况。

> 旧版本的「毛玻璃 / 半透明」档位已删除，不用再去切档位了。

**Q：毛玻璃会让界面变糊、看不清数字？**
A：拖动顶部的**「浮窗不透明度」滑块**往上（往 85% 方向），数字对比度会立刻恢复。
背后的模糊和 iOS 那层降饱和质感都不会因此消失。

**Q：浮窗太实了，想更透一点？**
A：把**「浮窗不透明度」滑块**往左拖（0% 最透）。注意：拖到很低时，
如果背后恰好是亮色窗口，白色数字可能不太看得清 —— 这是半透明的固有代价，
不是 bug。建议在浅色壁纸下保持在 50% 以上。

**Q：数据会一直存着吗？**
A：每个项目保留**最近 50 天**的记录，更早的会被自动清理。
这个长度覆盖了绝大多数众筹项目的完整周期。

**Q：为什么顶部没有文件/编辑/视图这些菜单？**
A：本应用不需要它们 —— 所有功能都在界面上了，去掉菜单栏能让窗口更清爽。

**Q：如何完全退出？**
A：右键系统托盘图标 → 退出。
**直接点窗口右上角的 × 只是最小化到托盘**，程序还在后台跑，这是有意设计的。

**Q：数据存在哪？会丢吗？**
A：数据保存在你自己的电脑上（用户目录下的应用数据文件夹），不会上传到任何服务器。
卸载时默认保留，重新装回来数据还在。

---

## 五、卸载

设置 → 应用 → 找到「Kickstarter Tracker」→ 卸载。
也可以从开始菜单里找卸载快捷方式。

---

## 六、给开发者的备注

如果你需要自己重新打包，在项目根目录执行：

```bash
npm install          # 首次需要
npm run icon         # 重新生成图标（改了 build/icon/gen-icon.js 后）
npm run dist         # 产出安装包到 dist/
```

打包依赖以下工具链（正常情况下 electron-builder 会自动下载，
但在访问 GitHub 受限的网络里需要预先放到缓存目录）：

```
%LOCALAPPDATA%\electron-builder\Cache\
├── nsis\                  （含 makensis.exe）
├── nsis-resources\
└── winCodeSign\           （含 rcedit-x64.exe）
```

`fetch-toolchain.js` 可以从国内镜像自动预取这三个包，用法：

```bash
node fetch-toolchain.js
```
